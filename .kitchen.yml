---
driver:
  # name: docker
  # use_sudo: false
  name: vagrant

provisioner:
  name: chef_zero

verifier:
  name: inspec

platforms:
  - name: ubuntu-16.04

suites:
  - name: default
    run_list:
      - recipe[aar_cookbook::web]
    verifier:
      inspec_tests:
        - test/recipes
    attributes:
